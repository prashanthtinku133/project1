name: Prometheus Monitoring Automation (Mock Mode)

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  monitoring:
    runs-on: ubuntu-latest

    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run Prometheus Monitoring (Mock)
        run: |
          python << 'EOF'
          import sys
          import random

          print("PROMETHEUS MONITORING JOB STARTED")
          print("Running in MOCK MODE (CI-safe)")

          # ---- MOCK TARGET CHECK ----
          print("\n--- TARGET HEALTH CHECK ---")
          targets = {
              "node1:9100": 1,
              "node2:9100": 1,
              "node3:910
